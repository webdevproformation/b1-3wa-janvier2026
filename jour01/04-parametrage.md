# Qui réalise les COMMIT ???


- `git -v` permet de savoir SI Git est bien installé sur votre ordinateur

# associer un NOM à votre git installé 

- dans VISUAL STUDIO afficher un terminal directement 

```sh
git config --global user.name "Malik H"
git config --global user.email "malik.h@webdevpro.net"
```

# verifier que c'est bon

```sh
git config --list
```